# Android Keystore Properties
# Copy this file to key.properties and fill in your actual values
# DO NOT commit key.properties to version control!

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
storeFile=/Users/YOUR_USERNAME/upload-keystore.jks

# Instructions:
# 1. Generate a keystore file using:
#    keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
#
# 2. Copy this file: cp key.properties.example key.properties
#
# 3. Update the values above with your actual keystore information
#
# 4. The key.properties file is already in .gitignore and will not be committed

